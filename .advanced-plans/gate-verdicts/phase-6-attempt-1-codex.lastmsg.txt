```json
{
  "phase": "phase-6",
  "attempt": 1,
  "timestamp": "2026-09-02T09:33:12+01:00",
  "agent": "codex",
  "backend": "codex",
  "verdict": "fail",
  "confidence": 99,
  "phase_title": "Advanced Planning Multi-Runtime Adapters",
  "findings": [
    {
      "severity": "critical",
      "location": "setup/{claude-code,codex,opencode,cursor}/install.sh; .advanced-plans/evidence/2026-09-01-four-host-skill-discovery.md",
      "description": "The adapters neither install nor discover the same named core-skill set.",
      "evidence": "setup/codex/install.sh:28-31, setup/opencode/install.sh:28-31, and setup/cursor/install.sh:28-31 deliberately exclude companion-detection and permission-config, while setup/claude-code/install.sh:576-590 copies every core/skills subdirectory. The four-host evidence additionally records OpenCode resolving Claude's surface, Claude resolving a global copy, and Cursor not listing phase-plan-creator."
    },
    {
      "severity": "critical",
      "location": "ralph-loop-004 and ralph-loop-005 fixture evidence",
      "description": "The required phase-plus-loop-plus-external-task fixture did not complete on every target host.",
      "evidence": ".advanced-plans/evidence/2026-08-28-loop-004-4-fixture-programme.md explicitly says check 1 passed only on OpenCode and Codex stage 2 was not run. .advanced-plans/evidence/2026-09-01-cursor-fixture-run.md:19-30 records that Cursor created the phase and loop but external-task-envelope.json and VALIDATION.txt were absent."
    },
    {
      "severity": "critical",
      "location": "platforms/python/evidence_gate.py; core/state/collected-evidence.schema.json; core/agents/worker.md; platforms/python/tests/test_scope_policy.py",
      "description": "ACC-08 is not enforced by an independent collector; the guard validates worker-authored path claims and its state-unchanged test cannot detect an illicit state write.",
      "evidence": "The schema says changed_paths must be independently computed, but core/agents/worker.md:212-254 instructs the worker to author the entire collected-evidence document, including git.changed_paths. evidence_gate.py:236-242 reads changed_paths directly from that document. test_scope_policy.py:342-373 merely calls the pure validate_path_scope function and compares a fixture file that no called code can modify, so the assertion passes even though no collection or advancement path was exercised."
    },
    {
      "severity": "critical",
      "location": "platforms/shared/agent-skills/advanced-planning/SKILL.md; platforms/claude-code/commands/next-loop.md; platforms/python/evidence_gate.py",
      "description": "Collected evidence is not wired through the joined schema-and-gate decision before state advancement.",
      "evidence": "The shared router used by Codex, OpenCode, and Cursor validates only loop-complete schema at SKILL.md:143-152 and only collected-evidence schema at 193-197; it never calls validate_advancement. Claude's next-loop.md:338-351 calls validate_loop_complete_advancement without verdict_paths, and evidence_gate.py:371-384 consequently manufactures a passing gate result. Repository-wide production-source search found validate_advancement only in evidence_gate.py's own docstring and definition; all executable callers are tests. The function also never checks collected-evidence policy booleans or invokes check_acc12_status."
    },
    {
      "severity": "critical",
      "location": "platforms/python/path_audit.py; .github/workflows/ci.yml",
      "description": "The CI path audit can pass while host-specific paths remain in unexamined parts of core/.",
      "evidence": "path_audit.py:140-154 scans only core/agents and core/skills, omitting canonical core/schemas and core/state. Its non-vacuity check at 446-450 requires only a nonzero total across all roots, not coverage of every core root. ci.yml:150-160 runs this default audit unchanged, so a host-specific token planted under core/state or core/schemas cannot make CI fail."
    }
  ],
  "loops_to_revert": [
    "ralph-loop-003",
    "ralph-loop-004",
    "ralph-loop-005",
    "ralph-loop-006"
  ],
  "failure_notes": [
    "Make the target-host skill set machine-derived from the canonical core set and require each real host to identify a canary from its intended project-local discovery surface; fail on missing, global, or alternate-surface resolution.",
    "Run the complete phase, loop, and validated external-task fixture on every target host. A recorded blocker is valid evidence but does not satisfy the phase success criterion.",
    "Implement a controller-side collector that independently computes git changed paths and check results, invokes the scope policy before any programme-state write, and tests the real collection-to-state transaction in both directions.",
    "Wire every collected-evidence advancement path to validate_advancement with the applicable gate inputs. Validate evidence policy outcomes and ACC-12 within that joined decision, and mutation-test the production call site.",
    "Audit all of core/ recursively or require explicit nonempty coverage for every canonical core root, with CI mutations under both core/state and core/schemas."
  ],
  "criteria_outcomes": [
    {
      "criterion": "Every target host discovers the same named core planning skills - not host-specific copies that drift.",
      "status": "failed",
      "evidence": "Claude Code installs all nine core skills, while Codex, OpenCode, and Cursor install seven and explicitly exclude two. The recorded host probes also resolved different physical surfaces or failed to list the project skill."
    },
    {
      "criterion": "A fixture programme can create one phase, one loop, and one external task on every target host.",
      "status": "failed",
      "evidence": "The OpenCode fixture completed, but the Codex fixture stopped before loop decomposition and envelope creation, and the Cursor fixture has an absent external-task-envelope.json and validation output."
    },
    {
      "criterion": "Only the control checkout updates programme state; a worker attempting a planning-state edit fails collection - ACC-08.",
      "status": "failed",
      "evidence": "No independent collector computes changed_paths or calls the scope guard. The implemented decision reads worker-authored changed_paths, while the state-unchanged tests exercise only a pure predicate and an otherwise untouched fixture."
    },
    {
      "criterion": "Collected evidence advances a loop only after both schema validation and gate validation pass.",
      "status": "failed",
      "evidence": "The collected-evidence decision has no production call site. The shared router performs schema validation only, and the sole Claude advancement call supplies no gate inputs, causing the gate half to default to pass."
    },
    {
      "criterion": "The CI path audit fails on any host-specific path in `core/`.",
      "status": "failed",
      "evidence": "CI scans core/agents and core/skills but not core/schemas or core/state, and its non-vacuity guard checks only the aggregate file count."
    },
    {
      "criterion": "No adapter duplicates a core skill's content; adapters install and register, they do not fork.",
      "status": "met",
      "evidence": "The Codex, OpenCode, and Cursor platform directories contain only adapter documentation; their installers source approved skills directly from core/skills and collision-check installed copies. The only platform SKILL.md files are the Cowork router and the single shared advanced-planning router, not host-specific copies of core skills."
    }
  ]
}
```