# v0.1 Smoke Report

**Status:** PARTIAL (REG-7 completed via static analysis; REG-1..REG-6 deferred to user-driven session)
**Date:** 2026-05-22
**Mode:** Headless PREP — interactive slash commands cannot execute without a live Claude Code session
**Test project:** `scratch/aaw-smoketest-20260522/` (empty git repo, no `.claude/`, no sub-packages)

---

## Headless Results

### REG-7 Static Analysis — Hook Scope Validation

**Source file analysed:** `references/settings-snippet.json` (loop-001 output)

**Matcher description:** `PostToolUse` hook with `matcher: "Write"`. After every Write tool call, a Node.js one-liner checks `CLAUDE_TOOL_INPUT_PATH`. It fires a console suggestion only if:
1. The path starts with `os.homedir() + '/.gstack/projects/'` (the prefix guard)
2. The basename matches `/-design-\d/` (the filename-pattern guard)

Both guards must pass for any output to appear. Any write that fails either guard causes the script to exit 0 silently — no interference with normal development.

| Sub-case | Input path | Verdict | Evidence / matcher field |
|---|---|---|---|
| (a) Hook SHOULD fire — design doc under ~/.gstack/projects/ | `~/.gstack/projects/MungoHarvey-Advanced-AI-Workflows/mharvey2-main-design-20260522-120000.md` | PASS | Guard 1: `p.startsWith(h + '/.gstack/projects/')` = true. Guard 2: `/-design-\d/.test('mharvey2-main-design-20260522-120000.md')` = true (matches `-design-2`). Console log fires with suggestion. |
| (b) Hook should NOT fire — path outside ~/.gstack/projects/ | `~/Documents/random/file.md` | PASS | Guard 1: `p.startsWith(h + '/.gstack/projects/')` = false. Script falls through. Exit 0 silently. Correct rejection. |
| (c) Hook should NOT fire — under ~/.gstack/projects/ but wrong filename pattern | `~/.gstack/projects/SomeProject/learnings.jsonl` | PASS | Guard 1 passes (path is under prefix). Guard 2: `/-design-\d/.test('learnings.jsonl')` = false. Script falls through. Exit 0 silently. Correct rejection — the filename pattern guard prevents spurious fires on non-design-doc writes. |
| (d) Idempotency — back-to-back writes to same design doc | Two sequential Write calls to the same `~/.gstack/projects/…-design-….md` path | PASS (with explanation) | The `PostToolUse` hook fires once per Write tool invocation — not twice per write. Two sequential Write calls produce two hook messages, which is expected PostToolUse semantics. Idempotency of `/gstack-to-plans` itself is guaranteed by gstack-to-plans SKILL.md Step 3: if destination content is identical to source, the skill exits as a no-op. `_merge_instructions.debounce_note` in settings-snippet.json explicitly documents this design decision. |

**REG-7 Overall Verdict: PASS**

The hook scope is correctly restricted. The two-guard design (prefix check + filename-pattern check) means:
- Writes inside `~/.gstack/projects/` to non-design-doc files do not trigger the suggestion
- Writes anywhere else are completely silent
- The hook cannot interfere with normal development workflows

**Windows note (from `_merge_instructions.windows_note`):** `os.homedir()` is cross-platform; on Windows it resolves to `%USERPROFILE%` correctly. No Windows-specific path issue found.

---

## User-Driven Checklist (REG-1..REG-6)

**Prerequisites before running:**
1. Open a fresh Claude Code session in the scratch test project: `scratch/aaw-smoketest-20260522/`
2. Ensure all four sub-packages are installed (gstack, advanced-planning, superpowers, plannotator)
3. Have a gstack project slug ready (e.g. `MyTestProject`)
4. Confirm `~/.gstack/projects/MyTestProject/` exists with at least one design doc matching `*-design-YYYYMMDD-HHmmss.md`

**How to open the scratch project in Claude Code:**
```
claude "scratch/aaw-smoketest-20260522/"
```
Or open a new Claude Code session with the scratch path as working directory.

---

### REG-1: Fresh-project E2E install and four-tool flow

Run in order inside the scratch project's Claude Code session:

- [ ] `/setup-with-claude` — expect: detection report listing which of the four tools are found; AskUserQuestion gates before writing CLAUDE.md routing block and before editing `.claude/settings.json`; `integrations.json` written to `.advanced-plans/`
- [ ] `/office-hours` (or equivalent gstack session) — expect: gstack creates a design doc in `~/.gstack/projects/{slug}/`; the PostToolUse hook fires and surfaces the `/gstack-to-plans` suggestion in Claude's next turn
- [ ] `/gstack-to-plans` — expect: design doc archived to `.advanced-plans/specs/{slug}-design-{datetime}.md`; AskUserQuestion fires if a same-named file already exists at destination
- [ ] `/plan-and-phase` — expect: `.advanced-plans/phases/phase-1/` directory created; `plan.md` and `loops.md` written with at least one loop stub
- [ ] `/next-loop` — expect: first loop's `loop-ready.json` written to `.advanced-plans/state/`
- [ ] `/run-gate` — expect: gate verdict written; `gate-verdict.md` or equivalent in `.advanced-plans/gate-verdicts/`
- [ ] `/next-phase` — expect: phase advancement or completion message

**Evidence to capture per step:** path of the artifact created, first 5–10 lines of content confirming correct structure.

---

### REG-2: Re-run idempotency

After REG-1, with no new gstack writes since the last `/gstack-to-plans`:

- [ ] Run `/gstack-to-plans` again — expect: no-op; Claude reports destination is identical to source; no file overwritten; no AskUserQuestion fired for content divergence

**Evidence:** Claude's output message confirming no-op; `ls -la .advanced-plans/specs/` shows unchanged timestamps.

---

### REG-3: Divergence detection

- [ ] Edit the archived spec in `.advanced-plans/specs/{slug}-design-{datetime}.md` (add or remove a line)
- [ ] Run `/gstack-to-plans` — expect: AskUserQuestion fires with 3 choices (overwrite / skip / view diff); no silent overwrite

**Evidence:** Capture the AskUserQuestion message text confirming it presents the 3 choices documented in the glue skill.

---

### REG-4: CLAUDE.md merge safety (routing block appears exactly once)

- [ ] Run `/setup-with-claude` a second time on the same scratch project — expect: Claude detects the existing `<!-- aaw-routing:begin -->` / `<!-- aaw-routing:end -->` markers; routing block is NOT duplicated; AskUserQuestion may ask whether to refresh; final CLAUDE.md contains the routing block exactly once

**Evidence:** `grep -c 'aaw-routing:begin' CLAUDE.md` returns `1`.

---

### REG-5: Clean uninstall

- [ ] Run `/setup-with-claude --uninstall` — expect: routing block removed from CLAUDE.md (via fenced markers); `.claude/skills/gstack-to-plans/` removed; `integrations.json` removed; `.advanced-plans/** ` permission entries removed from `.claude/settings.json`; sub-package installs (gstack, advanced-planning, superpowers, plannotator) are NOT touched

**Evidence:** `grep 'aaw-routing' CLAUDE.md` returns empty; sub-packages still present and functional.

---

### REG-6: Superpowers preference honoured

- [ ] Run a superpowers brainstorming session with the preference override set to `.advanced-plans/specs/`
- [ ] Expect: brainstorming output written to `.advanced-plans/specs/` (not to `docs/superpowers/plans/` or `.claude/plans/`)

**Evidence:** Path of the written file; confirm it is under `.advanced-plans/specs/`.

---

## Fix Iterations (todo-006-6)

This section is triggered only if any REG fails during the user-driven session above.

**Protocol on FAIL:**
1. Identify which loop (001/002/003) owns the failing artifact
2. Fix the artifact in that loop's output location
3. Re-run the affected REG
4. Update this report with the fix summary and re-test result

**Status:** Pending — not triggered yet. Activate this section if any REG above receives a FAIL verdict.

---

## Summary

| REG | Description | Verdict | Method |
|---|---|---|---|
| REG-1 | Fresh-project E2E install and four-tool flow | DEFERRED | Requires live session |
| REG-2 | Re-run idempotency (gstack-to-plans no-op) | DEFERRED | Requires live session |
| REG-3 | Divergence detection → AskUserQuestion | DEFERRED | Requires live session |
| REG-4 | CLAUDE.md routing block appears exactly once | DEFERRED | Requires live session |
| REG-5 | Clean uninstall (only meta artifacts removed) | DEFERRED | Requires live session |
| REG-6 | Superpowers preference to .advanced-plans/specs/ | DEFERRED | Requires live session |
| REG-7 | Auto-trigger hook scope (static analysis) | **PASS** | Static analysis of settings-snippet.json |

**Overall:** PARTIAL — REG-7 passes. REG-1..REG-6 require the user-driven checklist above to be completed in a fresh Claude Code session opened on `scratch/aaw-smoketest-20260522/`.
